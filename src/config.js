// ============================================================
//  全局配置
// ============================================================
module.exports = {
  // 【需要你补全】在线对战的 WebSocket 服务器地址。
  // 必须是 wss:// 开头，且域名需在「微信公众平台 -> 开发管理 -> 开发设置 -> 服务器域名 -> socket合法域名」中配置。
  // 你可以先部署本仓库 server/ 目录下的示例服务器（见 README.md），把域名填到这里。
  SERVER_URL: 'wss://your-server-domain.com/ws',

  // 本地缓存 key（用于记住昵称）
  STORAGE_KEY: 'cc_online_profile'
};
